---
// How It Works section for the landing page
import { Icon } from 'astro-icon/components';

const steps = [
  {
    icon: 'material-symbols:upload-file-outline-rounded',
    title: 'Paso 1: Comparte tu Juego',
    description: 'Nos envías la información y el acceso a tu juego o producto. Nosotros nos encargamos del resto.',
  },
  {
    icon: 'material-symbols:groups-outline-rounded',
    title: 'Paso 2: Seleccionamos Streamers',
    description: 'Buscamos en nuestra red los perfiles más activos y comprometidos que encajen con tu audiencia.',
  },
  {
    icon: 'material-symbols:rocket-launch-outline-rounded',
    title: 'Paso 3: Tu Marca al Aire',
    description: 'Coordinamos y lanzamos la campaña, dándote seguimiento personalizado y métricas en tiempo real.',
  },
];
---

<section id="how-it-works" class="py-20 px-4 animate-fade-in">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-text-light">¿Cómo funciona?</h2>
      <p class="text-text-muted mt-2">En solo 3 pasos, tu proyecto estará en vivo.</p>
    </div>
    <div class="grid md:grid-cols-3 gap-8 text-center relative">
      <!-- Línea de conexión (decorativa) -->
      <div class="hidden md:block absolute top-1/2 left-0 w-full h-0.5 bg-brand-card/30 transform -translate-y-1/2"></div>

      {steps.map((step, index) => (
        <div class="bg-brand-dark p-8 rounded-xl border border-brand-card transition-transform duration-300 hover:scale-105 hover:-translate-y-2 animate-fade-in relative z-10" style={`animation-delay:${index * 150}ms;`}>
          <div class="relative mb-6">
            <span class="absolute -top-4 -left-4 text-6xl font-black text-brand-card select-none">0{index + 1}</span>
            <div class="flex items-center justify-center h-20 w-20 rounded-full bg-brand-purple/20 mx-auto shadow-lg">
              <Icon name={step.icon} class="h-10 w-10 text-brand-pink" />
            </div>
          </div>
          <h3 class="text-xl font-bold mb-2 text-text-light">{step.title}</h3>
          <p class="text-text-muted">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
